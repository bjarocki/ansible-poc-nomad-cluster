template {
  source = "/etc/consul-templates/nginx-http-services.ctmpl"
  destination = "/etc/nginx/sites-enabled/consul-http-services.conf"
  command = "/bin/bash -c '/usr/sbin/nginx -s reload || true'"
}
